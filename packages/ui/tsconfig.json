{
	"extends": "@shared/tooling/typescript",
	"compilerOptions": {
		// UI-specific overrides for components, hooks and contexts
		// general
		"module": "ESNext",
		"moduleResolution": "bundler", // or "nodenext" or "bundler"
		"baseUrl": ".",

		"verbatimModuleSyntax": true,
		"noUncheckedIndexedAccess": true,
		"noImplicitOverride": true
	}
}

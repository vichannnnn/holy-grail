{
  "name": "backend",
  "private": true,
  "scripts": {
  "setup": "pip install -r backend/app/requirements.txt",
    "dev": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "format": "uv run ruff format",
    "test": "pytest app/tests -x -vv",
    "lint": "uv run ruff check ."
  }
}